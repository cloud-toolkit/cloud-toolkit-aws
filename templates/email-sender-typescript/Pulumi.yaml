name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: An Email Sender configuration
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
    identity: 
      description: The domain or address that will be set on the identity
    configureDNS:
      description: Configure the DNS records automatically. Set only to true if you are using a Domain Identity and it is registered using Route53 on this AWS account
      default: false
